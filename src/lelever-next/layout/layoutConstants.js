/**
 * Standard layout constants for all pages.
 * Use these so every page has the same parent container width and padding.
 */
export const CONTENT_MAX_W = '1440px';
export const CONTENT_PX = { base: 4, md: 6 };
